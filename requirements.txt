# Scraping (site web leboncoin via session navigateur)
curl-cffi>=0.15        # requêtes HTTP avec empreinte TLS d'un vrai navigateur
parsel>=1.9            # extraction du JSON __NEXT_DATA__
browser-cookie3>=0.20  # lecture des cookies (dont datadome) depuis Firefox

# Analyse par le LLM local (backend "ollama")
ollama>=0.6            # client du serveur Ollama
pydantic>=2.11         # sortie structurée validée (JSON)

# Analyse par un LLM distant via clé d'API (backend "api" : sans GPU, parallélisable)
instructor[anthropic,google-genai]>=1.15  # interface unique Anthropic/Gemini/OpenAI/Ollama + validation Pydantic
python-dotenv>=1.0           # charge la clé d'API depuis .env (jamais commité)

# Interface web locale (multi-recherches, lancement en un clic)
flask>=3.1             # petit serveur web local (formulaire + progression + résultats)
